server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # Necessário para o React Router funcionar (Client-side routing)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
